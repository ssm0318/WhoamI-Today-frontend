import styled from 'styled-components';
import { Layout } from '@design-system';

export const ToastContainer = styled(Layout.Absolute)`
  position: absolute;
  display: table;
  max-width: 76%;
  margin: auto;
`;
