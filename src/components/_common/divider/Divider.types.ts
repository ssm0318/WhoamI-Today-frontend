import { ColorKeys } from '../../../design-system/colors';

export type DividerProps = {
  horizontal?: boolean;
  width?: number;
  margin?: number;
  marginLeading?: number;
  marginTrailing?: number;
  bgColor?: ColorKeys;
};
