import styled from 'styled-components';
import { Layout } from '@design-system';

export const StyledModalHeader = styled(Layout.FlexCol)`
  position: sticky;
  top: 0;
  box-shadow: 0px 0.5px 0px 0px rgba(0, 0, 0, 0.3);
`;

export const StyledTitle = styled(Layout.FlexRow)`
  position: relative;
`;
