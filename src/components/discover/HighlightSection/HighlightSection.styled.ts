import styled from 'styled-components';
import { Colors } from '@design-system';

export const HighlightSectionWrapper = styled.div`
  display: flex;
  flex-direction: column;
  gap: 16px;
  background-color: ${Colors.PRIMARY};
`;
