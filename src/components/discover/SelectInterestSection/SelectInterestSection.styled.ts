import styled from 'styled-components';
import { Colors } from '@design-system';

export const SelectInterestSectionWrapper = styled.div`
  display: flex;
  flex-direction: column;
  gap: 16px;
  background-color: ${Colors.TERTIARY_PINK};
`;
